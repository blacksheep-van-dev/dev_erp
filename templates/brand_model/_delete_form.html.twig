<form method="post" action="{{ path('app_brand_model_delete', {'id': brand_model.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand_model.id) }}">
    <button class="btn">Delete</button>
</form>
