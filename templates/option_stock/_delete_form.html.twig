<form method="post" action="{{ path('app_option_stock_delete', {'id': option_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option_stock.id) }}">
    <button class="btn">Delete</button>
</form>
