{% extends 'base.html.twig' %}

{% block title %}OptionStock{% endblock %}

{% block body %}
    <h1>OptionStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_stock.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ option_stock.quantity }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ option_stock.enabled ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ option_stock.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_option_stock_index') }}">back to list</a>

    <a href="{{ path('app_option_stock_edit', {'id': option_stock.id}) }}">edit</a>

    {{ include('option_stock/_delete_form.html.twig') }}
{% endblock %}
