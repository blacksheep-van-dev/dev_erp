<form method="post" action="{{ path('app_insurance_delete', {'id': insurance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ insurance.id) }}">
    <button class="btn">Delete</button>
</form>
